name: Eighth Workflow
on:
  workflow_dispatch:
    inputs:
      titleCustom:
        description: Type the title
        type: string
        required: true
        default: "Hello, World!"

jobs:
  run-first:
    runs-on: ubuntu-latest
    steps:
      - name: Display event variables
        run: |
          echo $GITHUB_SHA
          echo $GITHUB_REF